Gets the basic data of this LCP known by the initial configuration.
---
summary: Getst the initial configuration for the LCP.
description: |
  Gets the initial configuration for the LCP. This includes information about itself
  like the execution environment ID (lcp.id), the URL where the LCP can be
  contacted and a human readable name.

tags: [self]
responses:
  200:
    description: Gets the LCP basic information.
    schema: LCPDescription
  201:
    description: Created
  400:
    description: Request not valid.
    schema: Bad_Request_Response_Schema
  401:
    description: Authentication failed.
    schema: Unauthorized_Response_Schema
  406:
    description: Request validation failed.
    schema: Not_Acceptable_Response_Schema
  500:
    description: Server not available to satisfy the request.
    schema: Internal_Server_Error_Response_Schema

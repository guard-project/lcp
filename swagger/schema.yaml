consumes:
- application/json
definitions:
  Agent:
    properties:
      description:
        description: Description of the Agent
        example: Example agent
        type: string
      endpoint_url:
        description: URL if some to configure the Parameters.
        example: http://example.com:4250/v1/config
        format: url
        type: string
      hasAgentType:
        description: Id of the Agent Type Associated with this Agent Instance
        example: PROBE
        type: string
      id:
        description: Security Function ID.
        example: 5db06770-8c64-4693-9724-ff318b02f897
        type: string
      partner:
        description: Name of the partner/owner of the Agent
        example: Guard Project
        type: string
      status:
        description: Update the status the of the agent-instance if the command is
          executed correctly.
        enum: &id001
        - started
        - stopped
        - unknown
        example: started
        type: string
      type:
        description: Data type AgentInstance. Must be 'AgentInstance'
        enum:
        - AgentInstance
        example: AgentInstance
        type: string
      vendor:
        description: Name of the vendor providing the security Function
        example: FIWARE Foundation e.V.
        type: string
      version:
        description: Version of the Security Property associated
        example: 1.0.1
        type: string
    required:
    - hasAgentType
    - id
    - status
    - type
    type: object
  AgentActionSchema:
    properties:
      cmd:
        description: Action command.
        example: service filebeat start
        type: string
      id:
        description: Action name
        example: start
        type: string
      status:
        description: Update the status the of the agent-instance according to cmd
          condition
        enum: *id001
        example: started
        type: string
    required:
    - cmd
    - id
    type: object
  AgentParameter:
    properties:
      description:
        description: Some description explaining the parameter
        example: Time between Heartbeats
        type: string
      list:
        description: Marks whether a parameter is list or not
        example: 'true'
        type: boolean
      path:
        description: Name of the Parameter
        example: HeartbeatTime
        type: string
      type:
        description: 'Type of the Parameter: Int, Number, String...'
        example: integer
        type: string
    required:
    - path
    - type
    type: object
  AgentResource:
    properties:
      description:
        description: Description of the Resource
        example: This is the config file for an agent
        type: string
      example:
        description: Config file example
        example: points to an example config file
        type: string
      path:
        description: Path of the Agent config file
        example: /etc/myagent/config.yaml
        type: string
    required:
    - path
    type: object
  AgentType:
    properties:
      actions:
        description: List of actions and expected result fro this agent
        items:
          $ref: '#/definitions/AgentActionSchema'
        type: array
      description:
        description: Description of the Agent type
        example: Example agent
        type: string
      id:
        description: Agent Type ID.
        example: 5db06770-8c64-4693-9724-ff318b02f897
        type: string
      jsonSchema:
        description: Pointer for the json describing the data from this kind of agents
        example: https://json-schema.org/draft/2020-12/schema
        type: string
      parameters:
        description: List of configuration parameters used
        items:
          $ref: '#/definitions/AgentParameter'
        type: array
      partner:
        description: Name of the Partner who created the resource
        example: guard-partner
        type: string
      resources:
        description: List of agents Resources that could be used
        items:
          $ref: '#/definitions/AgentResource'
        type: array
      schema:
        description: Type of configuration file (json, yaml, etc.)
        example: yaml
        type: string
      source:
        description: The config file where parameters are configured
        example: /etc/example_agent/config.yaml
        type: string
      type:
        description: Data type AgentType. Must be 'AgentType'
        enum:
        - AgentType
        example: AgentType
        type: string
    required:
    - actions
    - id
    - partner
    - schema
    - source
    - type
    type: object
  CloudCompute:
    properties:
      controller_id:
        description: ID of the Compute host. The controller could act as compute
        example: 3b3041bf-b2fb-47da-8e72-331a67ffd292
        type: string
      type:
        description: Type of server acting as CloudController
        example: BaremetalServer
        type: string
    required:
    - controller_id
    - type
    type: object
  CloudController:
    properties:
      controller_id:
        description: ID of the Controller host
        example: 3b3041bf-b2fb-47da-8e72-331a67ffd292
        type: string
      type:
        description: Type of server acting as CloudController
        example: BaremetalServer
        type: string
    required:
    - controller_id
    - type
    type: object
  CloudSchema:
    properties:
      compute_nodes:
        description: List of Cloud compute nodes
        items:
          $ref: '#/definitions/CloudCompute'
        type: array
      controllers:
        description: List of Cloud Controllers
        items:
          $ref: '#/definitions/CloudController'
        type: array
      description:
        description: Some description of the Cloud
        example: FIWARE Lab Wolfsburg node
        type: string
      id:
        description: Cloud ID
        example: f9e6ee65-517a-44da-854d-fdd058fcf2dd
        type: string
      name:
        description: Human readable Cloud name
        example: Wolfsburg-FIWARE-Lab-Cloud
        type: string
      type:
        description: Must be CloudDescription
        enum:
        - CloudDescription
        example: CloudDescription
        type: string
      vendor:
        description: Cloud Vendor Name
        example: Openstack
        type: string
      version:
        description: Cloud release code name
        example: queens
        type: string
    required:
    - id
    - name
    - type
    - vendor
    type: object
  ContainerSchema:
    properties:
      id:
        description: Id of the Container.
        example: 413216e3-169f-4638-830e-ef0607732fde
        type: string
      software:
        description: Description of the Software contained in the container
        items:
          $ref: '#/definitions/SoftwareDefinition'
        type: array
      technology:
        description: Description of the Container
        enum:
        - lxc
        - docker
        - k8s
        - rkt
        example: docker
        type: string
      type:
        description: Class SoftwareDefinition. The value must be ContainerDescription
        enum:
        - ContainerDescription
        example: ContainerDescription
        type: string
    required:
    - id
    - software
    - technology
    - type
    type: object
  ExecutionEnvironment:
    properties:
      environment:
        description: Definition of environment hardware. Can be one of the types LXCContainer,
          DockerContainer, VirtualServer, BaremetalServer
        type: object
      executionType:
        description: Type of Exec. Env. Deployment
        enum:
        - bare-metal
        - contailer-lxc
        - vm
        - container-k8s
        - container-docker
        - cloud
        - mobile
        - gateway
        - application
        example: bare-metal
        type: string
      id:
        description: This execution environment ID.
        example: bc2e2eff-fda1-45be-b7f1-93485b756470
        type: string
      type:
        description: Class ExecutionEnvironment. The value must be ExecutionEnvironment
        enum:
        - ExecutionEnvironment
        example: ExecutionEnvironment
        type: string
    required:
    - environment
    - executionType
    - id
    - type
    type: object
  ExternalStorageSchema:
    properties:
      description:
        description: High level description for this remote storage drive
        example: remote nfs drive
        type: string
      id:
        description: ID of this external Storage type
        example: a406874b-dea7-4cd1-9d4e-b82a18ec993b
        type: string
      storageType:
        description: Type of storage type.
        enum:
        - S3
        - NFS
        - iSCSI
        - Swift
        - CEPH
        - GlusterFS
        - SSH-FS
        example: NFS
        type: string
      type:
        description: Type of the document. It must be External Storage
        enum:
        - ExternalStorage
        example: ExternalStorage
        type: string
      url:
        description: URL for this remote resource
        example: nfs://192.168.10.245:2049/data
        type: string
    required:
    - id
    - storageType
    - type
    - url
    type: object
  InteractsWithSchema:
    properties:
      externalStorage:
        description: External storage devices description
        items:
          $ref: '#/definitions/ExternalStorageSchema'
        type: array
      softwareArtifacts:
        description: Software artifacts interacting with this LCP
        items:
          $ref: '#/definitions/SoftwareDefinition'
        type: array
    type: object
  LCPConnectionAsInCB:
    properties:
      father:
        description: ID of the LCP father.
        example: 0597a13e-9440-4139-a6dd-87eaa9799b55
        type: string
      https:
        description: Defines the protocol to connect the LCP. HTTPS if true, HTTP
          if false
        example: true
        type: boolean
      port:
        description: Port where the LCP is listening
        example: 4000
        type: integer
      sons:
        example:
        - lpc:son:id:1
        - lcp:son:id:2
        items:
          type: string
        type: array
    required:
    - https
    - port
    type: object
  LCPContextBrokerDefinition:
    properties:
      container:
        items:
          $ref: '#/definitions/ContainerSchema'
        type: array
      description:
        description: A Human readable description, so it is easy to identify
        example: Testing LCP in localhost
        type: string
      enabled:
        description: Describes if the LCP is enabled or not
        example: true
        type: boolean
      hostname:
        description: Hostname of the Host where the LCP can be contacted. If a proxy
          is behind the LCP, the value should be the proxy host
        example: localhost
        type: string
      hw_info:
        description: Definition of environment hardware. Can be one of the types LXCContainer,
          DockerContainer, VirtualServer, BaremetalServer
        type: object
      id:
        description: Unique ID for the Son LCP
        example: 15d41167-79d3-48bf-b8db-9db0c8cc8d54
        type: string
      interactions:
        $ref: '#/definitions/InteractsWithSchema'
      lcp:
        $ref: '#/definitions/LCPConnectionAsInCB'
      sw_info:
        items:
          $ref: '#/definitions/SoftwareDefinition'
        type: array
      type_id:
        description: Type of execution environment
        enum:
        - bare-metal
        - contailer-lxc
        - vm
        - container-k8s
        - container-docker
        - cloud
        - mobile
        - gateway
        - application
        example: bare-metal
        type: string
    required:
    - enabled
    - hostname
    - id
    - lcp
    - type_id
    type: object
  LCPDescription:
    properties:
      description:
        description: A Human readable description, so it is easy to identify
        example: Testing LCP in localhost
        type: string
      exec_env_type:
        description: execution environment type
        enum: &id002
        - LCPDescription
        example: bare-metal
        type: string
      id:
        description: Unique ID for the Son LCP
        example: 15d41167-79d3-48bf-b8db-9db0c8cc8d54
        type: string
      name:
        description: Name for the LCP in readable human format
        example: lcp-example
        type: string
      type:
        description: 'FIWARE''s type for this class: LCPDescription'
        enum: *id002
        example: LCPDescription
        type: string
      url:
        description: URL where the Son LCP Listens
        example: http://lcpapi.example.com:4000
        format: url
        type: string
    required:
    - id
    - name
    - type
    - url
    type: object
  LCPFatherConnection:
    properties:
      url:
        description: URL where the Son LCP Listens
        example: http://lcpapi.example.com:4000
        format: url
        type: string
    required:
    - url
    type: object
  PollSchema:
    properties:
      agentInstance:
        items:
          $ref: '#/definitions/Agent'
        type: array
      agentType:
        items:
          $ref: '#/definitions/AgentType'
        type: array
      exec_env:
        allOf:
        - $ref: '#/definitions/LCPContextBrokerDefinition'
        description: LCP Description as required by the CB
      lcpSons:
        items:
          $ref: '#/definitions/LCPContextBrokerDefinition'
        type: array
    required:
    - exec_env
    type: object
  SoftwareDefinition:
    properties:
      address:
        description: Server address or IP where the software is listening
        example: 192.168.0.1
        type: string
      hasSoftwareConnections:
        description: List of Known connected/related Software
        example: []
        items:
          type: string
        type: array
      id:
        description: ID of this Software Instance
        example: a406874b-dea7-4cd1-9d4e-b82a18ec993b
        type: string
      openTCPPorts:
        description: List of opened TCP Ports
        example:
        - 3306
        items:
          type: integer
        type: array
      openUDPPorts:
        description: List of opened UDP Ports
        example:
        - 33066
        items:
          type: integer
        type: array
      product:
        description: Name of this piece of Software instance
        example: MySQL
        type: string
      type:
        description: Class SoftwareDefinition. The value must be SoftwareDefinition
        enum:
        - SoftwareDefinition
        example: SoftwareDefinition
        type: string
      vendor:
        description: Name of the Vendor for this software
        example: Oracle Coorporation
        type: string
      version:
        description: Version of the Piece of Software installed
        example: 5.8.1
        type: string
    required:
    - id
    - product
    - type
    - version
    type: object
info:
  title: Local Control Plane
  version: 1.0.4
paths:
  /code:
    delete:
      description: Inject code at run-time in the local environment.
      parameters:
      - in: body
        name: payload
        required: true
        schema:
          items:
            $ref: '#/definitions/Code_Request_Schema'
          type: array
      responses:
        '200':
          description: Codes inject correctly.
          schema:
            oneOf:
            - items: Created_Response
              type: array
        '204':
          description: No content to inject codes based on the request.
          schema:
            $ref: '#/definitions/No_Content_Response_Schema'
        '400':
          description: Request not valid.
          schema:
            $ref: '#/definitions/Bad_Request_Response_Schema'
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '406':
          description: Request validation failed.
          schema:
            $ref: '#/definitions/Not_Acceptable_Response_Schema'
        '422':
          description: Not possible to inject one or more codes.
          schema:
            items:
              oneOf:
              - Created_Response_Schema
              - Unprocessable_Entity_Response_Schema
            type: array
          500:
            description: Server not available to satisfy the request.
            schema: Internal_Server_Error_Response_Schema
      summary: Code injection
      tags:
      - code
    post:
      description: Inject code at run-time in the local environment.
      parameters:
      - in: body
        name: payload
        required: true
        schema:
          items:
            $ref: '#/definitions/Code_Request_Schema'
          type: array
      responses:
        '200':
          description: Codes inject correctly.
          schema:
            oneOf:
            - items: Created_Response
              type: array
        '204':
          description: No content to inject codes based on the request.
          schema:
            $ref: '#/definitions/No_Content_Response_Schema'
        '400':
          description: Request not valid.
          schema:
            $ref: '#/definitions/Bad_Request_Response_Schema'
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '406':
          description: Request validation failed.
          schema:
            $ref: '#/definitions/Not_Acceptable_Response_Schema'
        '422':
          description: Not possible to inject one or more codes.
          schema:
            items:
              oneOf:
              - Created_Response_Schema
              - Unprocessable_Entity_Response_Schema
            type: array
          500:
            description: Server not available to satisfy the request.
            schema: Internal_Server_Error_Response_Schema
      summary: Code injection
      tags:
      - code
    put:
      description: Update the injected code at run-time in the local environment.
      parameters:
      - in: body
        name: payload
        required: true
        schema:
          items:
            $ref: '#/definitions/Code_Request_Schema'
          type: array
      responses:
        '200':
          description: All injected code correctly updated.
          schema:
            items:
              $ref: '#/definitions/Ok_Response_Schema'
            type: array
        '204':
          description: No content to update codes based on the request.
          schema:
            $ref: '#/definitions/No_Content_Response_Schema'
        '304':
          description: Update for one or more codes not necessary.
          schema:
            items:
              oneOf:
              - Ok_Response_Schema
              - Not_Modified_Response
            type: array
        '400':
          description: Request not valid.
          schema:
            $ref: '#/definitions/Bad_Request_Response_Schema'
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '406':
          description: Request validation failed.
          schema:
            $ref: '#/definitions/Not_Acceptable_Response_Schema'
        '415':
          description: Media type not supported.
          schema:
            $ref: '#/definitions/Unsupported_Media_Type_Response_Schema'
        '422':
          description: Not possible to update one or more codes based on the request.
          schema:
            items:
              oneOf:
              - Ok_Response_Schema
              - Not_Modified_Response
              - Unprocessable_Entity_Response_Schema
            type: array
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schema'
      summary: Update injected code
      tags:
      - code
  /code/{id}:
    delete:
      description: Inject code at run-time in the local environment.
      parameters:
      - in: body
        name: payload
        required: true
        schema:
          items:
            $ref: '#/definitions/Code_Request_Schema'
          type: array
      responses:
        '200':
          description: Codes inject correctly.
          schema:
            oneOf:
            - items: Created_Response
              type: array
        '204':
          description: No content to inject codes based on the request.
          schema:
            $ref: '#/definitions/No_Content_Response_Schema'
        '400':
          description: Request not valid.
          schema:
            $ref: '#/definitions/Bad_Request_Response_Schema'
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '406':
          description: Request validation failed.
          schema:
            $ref: '#/definitions/Not_Acceptable_Response_Schema'
        '422':
          description: Not possible to inject one or more codes.
          schema:
            items:
              oneOf:
              - Created_Response_Schema
              - Unprocessable_Entity_Response_Schema
            type: array
          500:
            description: Server not available to satisfy the request.
            schema: Internal_Server_Error_Response_Schema
      summary: Code injection
      tags:
      - code
    post:
      description: Inject code at run-time in the local environment.
      parameters:
      - in: body
        name: payload
        required: true
        schema:
          items:
            $ref: '#/definitions/Code_Request_Schema'
          type: array
      responses:
        '200':
          description: Codes inject correctly.
          schema:
            oneOf:
            - items: Created_Response
              type: array
        '204':
          description: No content to inject codes based on the request.
          schema:
            $ref: '#/definitions/No_Content_Response_Schema'
        '400':
          description: Request not valid.
          schema:
            $ref: '#/definitions/Bad_Request_Response_Schema'
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '406':
          description: Request validation failed.
          schema:
            $ref: '#/definitions/Not_Acceptable_Response_Schema'
        '422':
          description: Not possible to inject one or more codes.
          schema:
            items:
              oneOf:
              - Created_Response_Schema
              - Unprocessable_Entity_Response_Schema
            type: array
          500:
            description: Server not available to satisfy the request.
            schema: Internal_Server_Error_Response_Schema
      summary: Code injection
      tags:
      - code
    put:
      description: Update the injected code at run-time in the local environment.
      parameters:
      - in: body
        name: payload
        required: true
        schema:
          items:
            $ref: '#/definitions/Code_Request_Schema'
          type: array
      responses:
        '200':
          description: All injected code correctly updated.
          schema:
            items:
              $ref: '#/definitions/Ok_Response_Schema'
            type: array
        '204':
          description: No content to update codes based on the request.
          schema:
            $ref: '#/definitions/No_Content_Response_Schema'
        '304':
          description: Update for one or more codes not necessary.
          schema:
            items:
              oneOf:
              - Ok_Response_Schema
              - Not_Modified_Response
            type: array
        '400':
          description: Request not valid.
          schema:
            $ref: '#/definitions/Bad_Request_Response_Schema'
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '406':
          description: Request validation failed.
          schema:
            $ref: '#/definitions/Not_Acceptable_Response_Schema'
        '415':
          description: Media type not supported.
          schema:
            $ref: '#/definitions/Unsupported_Media_Type_Response_Schema'
        '422':
          description: Not possible to update one or more codes based on the request.
          schema:
            items:
              oneOf:
              - Ok_Response_Schema
              - Not_Modified_Response
              - Unprocessable_Entity_Response_Schema
            type: array
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schema'
      summary: Update injected code
      tags:
      - code
  /config:
    post:
      description: Apply configuration changes to the local environment.
      parameters:
      - in: body
        name: payload
        required: true
        schema:
          items:
            $ref: '#/definitions/Config_Request_Schema'
          type: array
      responses:
        '200':
          description: Configuration changed correctly.
          schema:
            oneOf:
            - items: Created_Response
              type: array
        '204':
          description: No content to configure based on the request.
          schema:
            $ref: '#/definitions/No_Content_Response_Schema'
        '400':
          description: Request not valid.
          schema:
            $ref: '#/definitions/Bad_Request_Response_Schema'
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '406':
          description: Request validation failed.
          schema:
            $ref: '#/definitions/Not_Acceptable_Response_Schema'
        '422':
          description: Not possible to change one or more configurations.
          schema:
            items:
              oneOf:
              - Created_Response_Schema
              - Unprocessable_Entity_Response_Schema
            type: array
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schema'
      summary: Configuration update
      tags:
      - config
  /status:
    get:
      description: Get info about the status of the LCP in the execution environment.
      responses:
        '200':
          description: Status data of the LCP.
          schema:
            $ref: '#/definitions/Status_Response_Schema'
        '400':
          description: Request not valid.
          schema:
            $ref: '#/definitions/Bad_Request_Response_Schema'
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '406':
          description: Request validation failed.
          schema:
            $ref: '#/definitions/Not_Acceptable_Response_Schema'
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schema'
      summary: Status info
      tags:
      - status
    post:
      description: Set the last heartbeat.
      responses:
        '200':
          description: Heartbeat with CB-Manager correctly executed.
          schema:
            $ref: '#/definitions/Status_Response_Schema'
        '400':
          description: Request not valid.
          schema:
            $ref: '#/definitions/Bad_Request_Response_Schema'
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '406':
          description: Request validation failed.
          schema:
            $ref: '#/definitions/Not_Acceptable_Response_Schema'
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schema'
      summary: Status set.
      tags:
      - status
  /self/software:
    get:
      description: 'Gets the information of the software that this LCP Knows. This
        data about software could be used

        by an external process to query the CVE database in search for known vulnerabilities
        after being

        retrieved.


        If no software is known by the LCP it would retrieve an empty array.

        '
      responses:
        '200':
          description: Get the Description of the Software controlled by the LCP
          schema:
            items:
              $ref: '#/definitions/SoftwareDefinition'
            type: array
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schema'
      summary: Get the Software known by this LCP to be installed together with this
        LCP
      tags:
      - software
    post:
      description: 'Posts the information of the Server software that LCP Knows. This
        data about software could be used

        by an external process to query the CVE database in search for known vulnerabilities
        after being

        retrieved from the LCP.

        '
      parameters:
      - in: body
        name: payload
        required: true
        schema:
          items:
            $ref: '#/definitions/SoftwareDefinition'
          type: array
      responses:
        '201':
          description: The Security Function definition has been successfully updated
            there.
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '406':
          description: Request validation failed.
          schema:
            $ref: '#/definitions/Not_Acceptable_Response_Schema'
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schema'
      summary: Post the Description and configuration parameters of Security Agents
        deployed with LCP
      tags:
      - software
  /self/software/{id}:
    delete:
      description: 'Gets the information of the software that this LCP Knows. This
        data about software could be used

        by an external process to query the CVE database in search for known vulnerabilities
        after being

        retrieved.


        If no software is known by the LCP it would retrieve an empty array.

        '
      responses:
        '200':
          description: Get the Description of the Software controlled by the LCP
          schema:
            items:
              $ref: '#/definitions/SoftwareDefinition'
            type: array
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schema'
      summary: Get the Software known by this LCP to be installed together with this
        LCP
      tags:
      - software
    get:
      description: 'Gets the information of the software that this LCP Knows. This
        data about software could be used

        by an external process to query the CVE database in search for known vulnerabilities
        after being

        retrieved.


        If no software is known by the LCP it would retrieve an empty array.

        '
      responses:
        '200':
          description: Get the Description of the Software controlled by the LCP
          schema:
            items:
              $ref: '#/definitions/SoftwareDefinition'
            type: array
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schema'
      summary: Get the Software known by this LCP to be installed together with this
        LCP
      tags:
      - software
    put:
      description: 'Posts the information of the Server software that LCP Knows. This
        data about software could be used

        by an external process to query the CVE database in search for known vulnerabilities
        after being

        retrieved from the LCP.

        '
      parameters:
      - in: body
        name: payload
        required: true
        schema:
          items:
            $ref: '#/definitions/SoftwareDefinition'
          type: array
      responses:
        '201':
          description: The Security Function definition has been successfully updated
            there.
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '406':
          description: Request validation failed.
          schema:
            $ref: '#/definitions/Not_Acceptable_Response_Schema'
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schema'
      summary: Post the Description and configuration parameters of Security Agents
        deployed with LCP
      tags:
      - software
  /self/container:
    get:
      description: 'Gets the information of a container (Docker, LXC, etc) running
        with the LCP and the information of

        the software that the container is running. This information could help to
        depict the deployment in

        the node and the Software information could be useful to query the CVE dtabase
        in search for known

        vulnerabilities.


        If no container is known by the LCP it would retrieve an empty array.

        '
      responses:
        '200':
          description: Get the Description of the containers and their software running
            with the LCP
          schema:
            items:
              $ref: '#/definitions/ContainerSchema'
            type: array
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schema'
      summary: Get Description of a container running with the LCP
      tags:
      - software
    post:
      description: 'Posts the information of a container (Docker, LXC, etc) running
        with the LCP and the information of

        the software that the container is running. This information could help to
        depict the deployment in

        the node and the Software information could be useful to query the CVE dtabase
        in search for known

        vulnerabilities.

        '
      parameters:
      - in: body
        name: payload
        required: true
        schema:
          items:
            $ref: '#/definitions/ContainerSchema'
          type: array
      responses:
        '201':
          description: The Security Function definition has been successfully updated
            there.
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '406':
          description: Request validation failed.
          schema:
            $ref: '#/definitions/Not_Acceptable_Response_Schema'
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schema'
      summary: Post the description of a container and the software installed inside
        it.
      tags:
      - software
  /self/container/{id}:
    delete:
      description: 'Gets the information of a container (Docker, LXC, etc) running
        with the LCP and the information of

        the software that the container is running. This information could help to
        depict the deployment in

        the node and the Software information could be useful to query the CVE dtabase
        in search for known

        vulnerabilities.


        If no container is known by the LCP it would retrieve an empty array.

        '
      responses:
        '200':
          description: Get the Description of the containers and their software running
            with the LCP
          schema:
            items:
              $ref: '#/definitions/ContainerSchema'
            type: array
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schema'
      summary: Get Description of a container running with the LCP
      tags:
      - software
    get:
      description: 'Gets the information of a container (Docker, LXC, etc) running
        with the LCP and the information of

        the software that the container is running. This information could help to
        depict the deployment in

        the node and the Software information could be useful to query the CVE dtabase
        in search for known

        vulnerabilities.


        If no container is known by the LCP it would retrieve an empty array.

        '
      responses:
        '200':
          description: Get the Description of the containers and their software running
            with the LCP
          schema:
            items:
              $ref: '#/definitions/ContainerSchema'
            type: array
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schema'
      summary: Get Description of a container running with the LCP
      tags:
      - software
  /lcp_son/{id}:
    delete:
      description: 'Removes one Son of the queried LCP given its ID.

        '
      parameters:
      - description: id of LCP son to be queried
        in: path
        name: id
        requiered: true
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Status data of the LCP.
          schema:
            $ref: '#/definitions/LCPDescription'
        '400':
          description: Request not valid.
          schema:
            $ref: '#/definitions/Bad_Request_Response_Schema'
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '406':
          description: Request validation failed.
          schema:
            $ref: '#/definitions/Not_Acceptable_Response_Schema'
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schema'
      summary: Removes an LCP Son given its ID
      tags:
      - lcp_relationships
    get:
      description: 'Gets one Son of the queried LCP given its ID.

        '
      parameters:
      - description: id of LCP son to be queried
        in: path
        name: id
        requiered: true
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Status data of the LCP.
          schema:
            $ref: '#/definitions/LCPDescription'
        '400':
          description: Request not valid.
          schema:
            $ref: '#/definitions/Bad_Request_Response_Schema'
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '406':
          description: Request validation failed.
          schema:
            $ref: '#/definitions/Not_Acceptable_Response_Schema'
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schema'
      summary: Get a LCP Son given its ID
      tags:
      - lcp_relationships
  /lcp_son:
    get:
      description: 'Gets a list of next links in the service chain (LCP sons) to a
        given execution environment.

        Following the LCP Sons the service chain can be quite accurately be depicted.

        '
      responses:
        '200':
          description: List of LCP Sons of the queried LCP.
          schema:
            items:
              $ref: '#/definitions/LCPDescription'
            type: array
        '400':
          description: Request not valid.
          schema:
            $ref: '#/definitions/Bad_Request_Response_Schema'
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '406':
          description: Request validation failed.
          schema:
            $ref: '#/definitions/Not_Acceptable_Response_Schema'
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schema'
      summary: Get the LCP Sons (next LCPs in Service Chain) of this LCP
      tags:
      - lcp_relationships
    post:
      description: 'Post a new LCP Son to a given LCP. Once the LCP knows a new children
        it could post an

        /lcp_parent operation to the children LCP so the children LCP can know which
        is its

        parent LCP and get data to connect the Context Broker.

        '
      parameters:
      - in: body
        name: payload
        required: true
        schema:
          $ref: '#/definitions/LCPDescription'
      responses:
        '201':
          description: Created
        '400':
          description: Request not valid.
          schema:
            $ref: '#/definitions/Bad_Request_Response_Schema'
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '406':
          description: Request validation failed.
          schema:
            $ref: '#/definitions/Not_Acceptable_Response_Schema'
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schema'
      summary: Add a new LCP Son to a given LCP, so a service chain can be depicted.
      tags:
      - lcp_relationships
  /lcp_parent:
    delete:
      description: 'Removes the LCP Parent of this LCP

        '
      responses:
        '202':
          description: Accepted for deletion
        '404':
          description: No parent found.
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schema'
      summary: Removes the LCP Parent of this LCP.
      tags:
      - lcp_relationships
    get:
      description: 'Gets a list of previous links in the service chain (LCP parents)
        to a given execution environment.

        '
      responses:
        '200':
          description: List of LCP Sons of the queried LCP.
          schema:
            items:
              $ref: '#/definitions/LCPFatherConnection'
            type: array
        '400':
          description: Request not valid.
          schema:
            $ref: '#/definitions/Bad_Request_Response_Schema'
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '406':
          description: Request validation failed.
          schema:
            $ref: '#/definitions/Not_Acceptable_Response_Schema'
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schema'
      summary: Get the LCP parent of a LCP
      tags:
      - lcp_relationships
    post:
      description: 'A LCP presents itself as the parent to another LCP. Once the LCP
        knows its parent, it can

        register itself using /lcp_son POST to the parent LCP, and getting back the
        Context Broker

        connection data.

        '
      parameters:
      - in: body
        name: payload
        required: true
        schema:
          $ref: '#/definitions/LCPFatherConnection'
      responses:
        '201':
          description: Created
        '400':
          description: Request not valid.
          schema:
            $ref: '#/definitions/Bad_Request_Response_Schema'
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '406':
          description: Request validation failed.
          schema:
            $ref: '#/definitions/Not_Acceptable_Response_Schema'
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schema'
      summary: Add the LCP parent to a given LCP. Once the LCP Son knows its parent,
        it can connect it so it can get the Context Broker connection data.
      tags:
      - lcp_relationships
  /cloud:
    get:
      description: 'Gets the information of the cloud Infrastructure where the LCP
        is installed on. This description of

        the cloud infrastructure could define as inner objects also things like:

        * Controllers - Used to control the cloud infrastrucutre

        * Compute Nodes - Used to control the Virtual Servers


        Although this information will only be informative, not specially relevant.

        '
      responses:
        '200':
          description: Get the description of the Cloud Infrastructure known by the
            system
          schema:
            items:
              $ref: '#/definitions/CloudSchema'
            type: array
        '400':
          description: Request not valid.
          schema:
            $ref: '#/definitions/Bad_Request_Response_Schema'
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '406':
          description: Request validation failed.
          schema:
            $ref: '#/definitions/Not_Acceptable_Response_Schema'
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schema'
      summary: Get the Description of the Cloud Infrastructure
      tags:
      - hardware
    post:
      description: 'Posts the information of the cloud Infrastrucutre where the LCP
        is installed on.

        '
      parameters:
      - in: body
        name: payload
        required: true
        schema:
          items:
            $ref: '#/definitions/CloudSchema'
          type: array
      responses:
        '201':
          description: The Cloud Infrastructure has been successfully updated
        '400':
          description: Request not valid.
          schema:
            $ref: '#/definitions/Bad_Request_Response_Schema'
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '406':
          description: Request validation failed.
          schema:
            $ref: '#/definitions/Not_Acceptable_Response_Schema'
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schema'
      summary: Post the Description of the Cloud Infrastructure where the LCP is installed
        on.
      tags:
      - hardware
  /agent/instance:
    get:
      description: 'Gets the information of the SecurityFunctions deployed along with
        the LCP

        '
      responses:
        '200':
          description: Get the Server's configuration infromation from LCP
          schema:
            $ref: '#/definitions/Agent'
        '400':
          description: Request not valid.
          schema:
            $ref: '#/definitions/Bad_Request_Response_Schema'
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '406':
          description: Request validation failed.
          schema:
            $ref: '#/definitions/Not_Acceptable_Response_Schema'
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schema'
      summary: Get the Description and configuration parameters of Security Agents
        deployed with LCP
      tags:
      - Agents
    post:
      description: 'Post the description of an instance known its Agent type.

        '
      parameters:
      - in: body
        name: payload
        required: true
        schema:
          items:
            $ref: '#/definitions/Agent'
          type: array
      responses:
        '201':
          description: The Agent Instance has been successfully updated there.
        '400':
          description: Request not valid.
          schema:
            $ref: '#/definitions/Bad_Request_Response_Schema'
        '404':
          description: Agent type not previously defined. The agent type should be
            defined before the Instance.
          schema:
            $ref: '#/definitions/Not_Found_Response'
        '406':
          description: Request validation failed.
          schema:
            $ref: '#/definitions/Not_Acceptable_Response_Schema'
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schem'
      summary: Post the description of an instance of an Agent type previously defined.
      tags:
      - Agents
  /agent/type:
    get:
      description: 'Gets the information of the Agent Types known by this LCP and
        the data got.

        '
      responses:
        '200':
          description: Data relative to the Agents known by this LCP agent
          schema:
            $ref: '#/definitions/AgentType'
        '400':
          description: Request not valid.
          schema:
            $ref: '#/definitions/Bad_Request_Response_Schema'
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '406':
          description: Request validation failed.
          schema:
            $ref: '#/definitions/Not_Acceptable_Response_Schema'
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schema'
      summary: Get data relative to the Security Agents defined to be used this LCP
      tags:
      - Agents
    post:
      description: 'Posts the information of an Agent Type that so that an agent can
        register later to belong this kind

        '
      parameters:
      - in: body
        name: payload
        required: true
        schema:
          items:
            $ref: '#/definitions/AgentType'
          type: array
      responses:
        '201':
          description: The software definition has been successfully updated there.
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '406':
          description: Request validation failed.
          schema:
            $ref: '#/definitions/Not_Acceptable_Response_Schema'
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schem'
      summary: Post an Agent type
      tags:
      - Agents
  /agent/instance/{id}:
    delete:
      description: 'Remove the description of a Security Agent Instance deployed with
        LCP.

        '
      parameters:
      - description: id of Agent Instance to be deleted
        in: path
        name: id
        requiered: true
        required: true
        schema:
          type: string
      responses:
        '204':
          description: Removed successfuly.
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '404':
          description: Agent Instance not found.
        '406':
          description: Request validation failed.
          schema:
            $ref: '#/definitions/Not_Acceptable_Response_Schema'
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schema'
      summary: Remove the description of an Agent Instance deployed with LCP
      tags:
      - Agents
    get:
      description: 'Gets the information of the SecurityFunctions deployed along with
        the LCP

        '
      responses:
        '200':
          description: Get the Server's configuration infromation from LCP
          schema:
            $ref: '#/definitions/Agent'
        '400':
          description: Request not valid.
          schema:
            $ref: '#/definitions/Bad_Request_Response_Schema'
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '406':
          description: Request validation failed.
          schema:
            $ref: '#/definitions/Not_Acceptable_Response_Schema'
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schema'
      summary: Get the Description and configuration parameters of Security Agents
        deployed with LCP
      tags:
      - Agents
    put:
      description: 'Updates the description of an instance known and previously defined.

        '
      parameters:
      - description: id of Agent Instance to be updated
        in: path
        name: id
        requiered: true
        required: true
        schema:
          type: string
      - in: body
        name: payload
        required: true
        schema:
          items:
            $ref: '#/definitions/Agent'
          type: array
      responses:
        '201':
          description: The Agent Instance has been successfully updated there.
        '400':
          description: Request not valid.
          schema:
            $ref: '#/definitions/Bad_Request_Response_Schema'
        '404':
          description: Agent instance not previously defined. Use POST method instead.
          schema:
            $ref: '#/definitions/Not_Found_Response'
        '406':
          description: Request validation failed.
          schema:
            $ref: '#/definitions/Not_Acceptable_Response_Schema'
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schem'
      summary: Puts the description of an instance of an Agent instance previously
        defined.
      tags:
      - Agents
  /agent/type/{id}:
    delete:
      description: 'Deletes an agent type so it can not be used anymore to deploy
        Agent Instances. If there are known agent instances of this type, it will
        fail to delete the type. Instances should be deleted first.

        '
      parameters:
      - description: id of Agent type to be modified
        in: path
        name: id
        requiered: true
        required: true
        schema:
          type: string
      responses:
        '204':
          description: The Agent Type has been successfully deleted.
        '404':
          description: The Agent Type was not found. No deletions performed.
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '406':
          description: Request validation failed.
        '412':
          description: Precondition failed. There are instances of one type yet, they
            must be deleted first
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schem'
      summary: Deletes an Agent type.
      tags:
      - Agents
    get:
      description: 'Gets the information of the Agent Types known by this LCP and
        the data got.

        '
      responses:
        '200':
          description: Data relative to the Agents known by this LCP agent
          schema:
            $ref: '#/definitions/AgentType'
        '400':
          description: Request not valid.
          schema:
            $ref: '#/definitions/Bad_Request_Response_Schema'
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '406':
          description: Request validation failed.
          schema:
            $ref: '#/definitions/Not_Acceptable_Response_Schema'
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schema'
      summary: Get data relative to the Security Agents defined to be used this LCP
      tags:
      - Agents
    put:
      description: 'Updates the information about an Agent Type that so that an agent
        can register later to belong this kind

        '
      parameters:
      - description: id of Agent type to be modified
        in: path
        name: id
        requiered: true
        required: true
        schema:
          type: string
      - in: body
        name: payload
        required: true
        schema:
          items:
            $ref: '#/definitions/AgentType'
          type: array
      responses:
        '201':
          description: The Agent Type has been successfully updated there.
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '406':
          description: Request validation failed.
          schema:
            $ref: '#/definitions/Not_Acceptable_Response_Schema'
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schem'
      summary: Put an Agent type
      tags:
      - Agents
  /self/deployment:
    get:
      description: 'Gets the information of the Server where the LCP is deployed.

        '
      responses:
        '200':
          description: 'Gets the description of the execution environment. The value
            for the

            environment return value could be one of BaremetalServer, LXC Container,

            Docker container, VirtualServer depending on the value for the "type"

            of the execution environment.

            '
          schema:
            $ref: '#/definitions/ExecutionEnvironment'
        '400':
          description: Request not valid.
          schema:
            $ref: '#/definitions/Bad_Request_Response_Schema'
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '406':
          description: Request validation failed.
          schema:
            $ref: '#/definitions/Not_Acceptable_Response_Schema'
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schema'
      summary: Get the execution environment deployment for the LCP
      tags:
      - self
    post:
      description: 'Post the description of the execution environment. The value for
        the

        environment value could be one of BaremetalServer, LXC Container,

        Docker container, VirtualServer depending on the value for the "type"

        of the execution environment.

        '
      parameters:
      - in: body
        name: payload
        required: true
        schema:
          $ref: '#/definitions/ExecutionEnvironment'
      responses:
        '201':
          description: Created
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '406':
          description: Request validation failed.
          schema:
            $ref: '#/definitions/Not_Acceptable_Response_Schema'
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schema'
      summary: Post the description of the Deployment for the execution environment
        overriding that calculated
      tags:
      - self
  /self:
    get:
      description: 'Gets the initial configuration for the LCP. This includes information
        about itself

        like the execution environment ID (lcp.id), the URL where the LCP can be

        contacted and a human readable name.

        '
      responses:
        '200':
          description: Gets the LCP basic information.
          schema:
            $ref: '#/definitions/LCPDescription'
        '201':
          description: Created
        '400':
          description: Request not valid.
          schema:
            $ref: '#/definitions/Bad_Request_Response_Schema'
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '406':
          description: Request validation failed.
          schema:
            $ref: '#/definitions/Not_Acceptable_Response_Schema'
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schema'
      summary: Getst the initial configuration for the LCP.
      tags:
      - self
  /self/configuration:
    post:
      description: 'Post initial configuration for the LCP. This includes information
        about itself

        like the execution environment ID (lcp.id), the URL where the LCP can be

        contacted and a human readable name.


        This request will trigger a "self discovery" of the deployment of the

        execution environment (baremental, container, vm), network configuration,

        disks, cpus, ram, etc

        '
      parameters:
      - in: body
        name: payload
        required: true
        schema:
          $ref: '#/definitions/LCPDescription'
      responses:
        '201':
          description: Created
        '400':
          description: Request not valid.
          schema:
            $ref: '#/definitions/Bad_Request_Response_Schema'
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '406':
          description: Request validation failed.
          schema:
            $ref: '#/definitions/Not_Acceptable_Response_Schema'
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schema'
      summary: Post initial configuration for the LCP.
      tags:
      - self
  /interactions:
    get:
      description: 'Gets a description of the components known that the host is working
        with according to a classical client/server

        paradigm. This includes relationship with other components like publish-subscribe,
        connection to databases,

        external storage drives (NFS, iSCSI, Dropbox), etc.

        '
      responses:
        '200':
          description: Get the Server's configuration infromation from LCP
          schema:
            $ref: '#/definitions/InteractsWithSchema'
        '400':
          description: Request not valid.
          schema:
            $ref: '#/definitions/Bad_Request_Response_Schema'
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '406':
          description: Request validation failed.
          schema:
            $ref: '#/definitions/Not_Acceptable_Response_Schema'
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schema'
      summary: Gets the description of the known elements that a host with a LCP is
        interacting with.
      tags:
      - interacts
    post:
      description: 'Post a description of the components that the host is working
        with according to a classical client/server

        paradigm. This includes relationship with other components like publish-subscribe,
        connection to databases,

        external storage drives (NFS, iSCSI, Dropbox), etc.

        '
      parameters:
      - in: body
        name: payload
        required: true
        schema:
          $ref: '#/definitions/InteractsWithSchema'
      responses:
        '201':
          description: The external components which our node is interacting with
            has been successfully updated.
        '400':
          description: Request not valid.
          schema:
            $ref: '#/definitions/Bad_Request_Response_Schema'
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '406':
          description: Request validation failed.
          schema:
            $ref: '#/definitions/Not_Acceptable_Response_Schema'
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schem'
      summary: Post the description of the elements a host with an LCP is interacting
        with.
      tags:
      - interacts
  /interactions/{id}:
    delete:
      description: 'Deletes a description of the components known that the host was
        working with according to a classical client/server

        paradigm.

        '
      parameters:
      - description: id of element to query.
        in: path
        name: id
        requiered: true
        required: true
        schema:
          type: string
      responses:
        '204':
          description: The resource has been correctly deleted
        '400':
          description: Request not valid.
          schema:
            $ref: '#/definitions/Bad_Request_Response_Schema'
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '404':
          description: Id Not Found
        '406':
          description: Request validation failed.
          schema:
            $ref: '#/definitions/Not_Acceptable_Response_Schema'
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schema'
      summary: Deletes the description of a known element that a host with a LCP is
        interacting with.
      tags:
      - interacts
    get:
      description: 'Gets a description of the components known that the host is working
        with according to a classical client/server

        paradigm. This includes relationship with other components like publish-subscribe,
        connection to databases,

        external storage drives (NFS, iSCSI, Dropbox), etc.

        '
      parameters:
      - description: id of element to query.
        in: path
        name: id
        requiered: true
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Get the Server's configuration infromation from LCP, but only
            the required ID.
          schema:
            $ref: '#/definitions/InteractsWithSchema'
        '400':
          description: Request not valid.
          schema:
            $ref: '#/definitions/Bad_Request_Response_Schema'
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '404':
          description: Id Not Found
        '406':
          description: Request validation failed.
          schema:
            $ref: '#/definitions/Not_Acceptable_Response_Schema'
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schema'
      summary: Gets the description of the known elements that a host with a LCP is
        interacting with.
      tags:
      - interacts
    put:
      description: 'UPdates a description of the components known that the host was
        working with according to a classical client/server

        paradigm.

        '
      parameters:
      - description: id of element to query.
        in: path
        name: id
        requiered: true
        required: true
        schema:
          type: string
      responses:
        '204':
          description: The resource has been correctly updated
        '400':
          description: Request not valid.
          schema:
            $ref: '#/definitions/Bad_Request_Response_Schema'
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '404':
          description: Id Not Found
        '406':
          description: Request validation failed.
          schema:
            $ref: '#/definitions/Not_Acceptable_Response_Schema'
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schema'
      summary: Updates the description of a known element that a host with a LCP is
        interacting with.
      tags:
      - interacts
  /poll:
    get:
      description: Gets all data the LCP has about itself and its environment. Gets
        all data the LCP has, specially to the Context Broker, so the CB can integrate
        this data in its knownledge base. The Context Broker will be the one to call
        this method since there is no pushing information to the CB from the LCP.
        This is so mainly for security reasons.
      responses:
        '200':
          description: retrieves all info the LCP has about itself.
          schema:
            $ref: '#/definitions/PollSchema'
        '404':
          description: Unconfigured LCP. No data to be retrieved this way.
          schema:
            $ref: '#/definitions/Bad_Request_Response_Schema'
        '401':
          description: Authentication failed.
          schema:
            $ref: '#/definitions/Unauthorized_Response_Schema'
        '500':
          description: Server not available to satisfy the request.
          schema:
            $ref: '#/definitions/Internal_Server_Error_Response_Schema'
      summary: null
      tags:
      - cb integration
produces:
- application/json
swagger: '2.0'
tags:
- description: Code injection at run-time.
  name: code
- description: Configuration at run-time.
  name: config
- description: Status data of the LCP.
  name: status
- description: Returns the description of software installed
  name: software
- description: Returns the description of software installed
  name: software
- description: Returns the description of software installed in containers
  name: software
- description: Returns the description of software installed in containers
  name: software
- description: Describes a "son" LCP linked in this service chain.
  name: lcp_relationships
- description: Describes a "son" LCP linked in this service chain.
  name: lcp_relationships
- description: Describes a "son" LCP linked in this service chain.
  name: lcp_relationships
- description: Description of a Cloud Infrastructure
  name: hardware
- description: Describes the Agent types and Agent instances.
  name: Agents
- description: Describes the Agent types and Agent instances.
  name: Agents
- description: Describes the Agent types and Agent instances.
  name: Agents
- description: Describes the Agent types and Agent instances.
  name: Agents
- description: This method does the initial configuration
  name: self
- description: Returns description of self LCP.
  name: self
- description: Initial configuration for the LCP
  name: self
- description: Describes interactions with other external resources.
  name: interacts
- description: Describes interactions with other external resources.
  name: interacts
- description: Retrieves data from LCP to the Context Broker
  name: cb integration
